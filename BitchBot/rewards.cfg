ballTouchReward         = 0.8      # r per sec touching ball (ground level)
ballAccelerateReward    = 1.2      # r per 0->79.2km/h ball velocity
ballTowardGoal          = 0.6      # r per sec with ball toward goal
goalReward              = 4.0      # r per goal
saveReward              = 2.5      # r per save
demoReward              = 1.2      # r per demo
aquireBoostReward       = 1.5      # r per 0->100 boost
saveBoostReward         = 0.25     # r per sec with 100 boost
rewardShare             = 0.75     # r shared between temmates
opponentNegation        = 0.85     # r negation for avg. opponent rewards
defendingReward         = 0.19     # r for being in defending position
attackingReward         = 0.14     # r for being in attacking position
toDefenceReward         = 0.15     # r for driving toward defense if on wrong side of ball
