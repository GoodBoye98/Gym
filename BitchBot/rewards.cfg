ballTouchReward         = 0.80     # r per sec touching ball (ground level)
inAirMultiplier         = 3.00     # multiplier for touching ball in the air
ballAccelerateReward    = 1.40     # r per 0->79.2km/h ball velocity
ballTowardGoal          = 0.40     # r per sec with ball toward goal
goalReward              = 8.00     # r per goal
saveReward              = 1.50     # r per save
demoReward              = 1.00     # r per demo
aquireBoostReward       = 2.00     # r per 0->100 boost
saveBoostReward         = 0.50     # r per sec with 100 boost
rewardShare             = 0.80     # r shared between temmates
opponentNegation        = 1.00     # r negation for avg. opponent rewards
defendingReward         = 0.12     # r for being in defending position
attackingReward         = 0.08     # r for being in attacking position
faceAerialBall          = 3.50     # r per sec for flying toward ball in the air
closeToAerialBall       = 3.50     # r per sec for being close to ball in the air
flipResetReward         = 4.00     # r for obtaining a flip reset in the air
