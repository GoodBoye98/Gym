ballTouchReward         = 0.80     # r per sec touching ball (ground level)toDefenceReward
inAirMultiplier         = 5.00     # multiplier for touching ball in the air
ballAccelerateReward    = 1.30     # r per 0->79.2km/h ball velocity
ballTowardGoal          = 0.15     # r per sec with ball toward goal
goalReward              = 3.00     # r per goal
saveReward              = 1.50     # r per save
demoReward              = 1.00     # r per demo
aquireBoostReward       = 1.40     # r per 0->100 boost
saveBoostReward         = 0.20     # r per sec with 100 boost
rewardShare             = 0.60     # r shared between temmates
opponentNegation        = 1.00     # r negation for avg. opponent rewards
defendingReward         = 0.08     # r for being in defending position
attackingReward         = 0.06     # r for being in attacking position
toDefenceReward         = 0.03     # r for driving toward defense if on wrong side of ball
flyTowardAerialBall     = 1.00     # r for flying toward ball in the air
closeToAerialBall       = 1.00     # r for being close to ball in the air
